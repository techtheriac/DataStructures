using System;
using System.Collections.Generic;
using System.Text;

namespace DataStructures
{
    public class Queue<T>
    {
        // Operations allowable
        // IsEmpty() returns true if the queue is empty or converse
        // Enqueue(T item) adds an item to the tail of the queue
        // Dequeue(T item) removes and return the item at the head of the queue
        // Size() shows the number of items currently in the queue
    }
}
