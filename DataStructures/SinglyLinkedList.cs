using System;
using System.Collections.Generic;
using System.Text;

namespace DataStructures
{
    public class SinglyLinkedList<T>
    {
        // Operations allowable
        // Add(T item) adds an item to the tail and returns the linkedList size
        // Remove(T item) removes the first occurence of an item in the linkedList,
        // returns true if the said item is found and removed or converse
        // Check(T item) checks for a specific item in the linked list, return true or false
        // Index(T item) returns the index of an item or returns -1 if the item isn't found
    }
}
